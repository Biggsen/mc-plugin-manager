/**
 * Shared YAML stringify options for consistent output across generators.
 */
export const YAML_STRINGIFY_OPTIONS = {
  indent: 2,
  lineWidth: 0,
  singleQuote: true,
  simpleKeys: false,
} as const
